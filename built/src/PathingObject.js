export class PathingObject {
    constructor(wayPoints, track, wayPointsIndex, position) {
        this.wayPoints = wayPoints;
        this.track = track;
        this.wayPointsIndex = wayPointsIndex;
        this.position = position;
    }
}
